const firstWord = "bird";
const secondWord = "something";

if (firstWord.length > secondWord.length) {
	console.log(`${firstWord} is longer than ${secondWord}`);
} else {
	console.log(`${secondWord} is longer than ${firstWord}`);
}
